import { StyleSheet } from 'react-native';

// Styles for the ValidationErrors component are centralized here
// to keep the JSX minimal and enable reuse/theming.
export const styles = StyleSheet.create({
  text: {
    color: '#ff6b6b',
    fontSize: 14,
    marginBottom: 10,
  },
});

export default styles;